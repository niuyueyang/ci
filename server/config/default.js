// default.js
// 设置配置文件
const config = {
    // 启动端口
    port: 3000,

    // 数据库配置
    database: {
        DATABASE: 'gitlab',
        USERNAME: 'root',
        PASSWORD: 'asdf123456A*',
        PORT: '3306',
        HOST: '39.106.10.163'
    }
}

module.exports = config
